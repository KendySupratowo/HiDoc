<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <title>Edit Profil - HiDoc</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans">

    <!-- Navbar -->
    <nav class="bg-green-200 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/home">
                <h1 class="text-2xl font-bold text-green-700 hover:text-green-900">HiDoc</h1>
            </a>
            <div class="space-x-4">
                <a href="/profile" class="text-green-800 font-semibold hover:text-green-900 font-bold">Halo, <%=
                        user.username %>!</a>
                <a href="/diagnose/history" class="text-green-700 hover:text-green-900 font-bold">Riwayat</a>
                <a href="/logout" class="text-green-700 hover:text-green-900 font-bold">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Form Edit Profil -->
    <section class="py-12">
        <div class="max-w-xl mx-auto bg-white rounded-lg shadow-md p-8">
            <h2 class="text-2xl font-bold text-green-700 mb-6 text-center">Edit Profil</h2>
            <form action="/profile/edit" method="POST" class="space-y-4">

                <div>
                    <label class="block text-gray-700 font-semibold">Nama Lengkap</label>
                    <input type="text" name="name" value="<%= profile.name %>" required
                        class="w-full mt-1 px-4 py-2 border rounded-md shadow-sm">
                </div>

                <div>
                    <label class="block text-gray-700 font-semibold">Usia</label>
                    <input type="number" name="age" value="<%= profile.age %>" required
                        class="w-full mt-1 px-4 py-2 border rounded-md shadow-sm">
                </div>

                <div>
                    <label class="block text-gray-700 font-semibold">Jenis Kelamin</label>
                    <select name="gender" required class="w-full mt-1 px-4 py-2 border rounded-md shadow-sm">
                        <option value="Laki-laki" <%=profile.gender==='Laki-laki' ? 'selected' : '' %>>Laki-laki
                        </option>
                        <option value="Perempuan" <%=profile.gender==='Perempuan' ? 'selected' : '' %>>Perempuan
                        </option>
                    </select>
                </div>

                <div class="text-center">
                    <button type="submit"
                        class="bg-green-500 hover:bg-green-600 text-white font-semibold px-6 py-2 rounded-md">
                        Simpan Perubahan
                    </button>
                </div>

            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-green-200 py-6 mt-12">
        <div class="container mx-auto text-center">
            <p class="text-green-700">© 2025 HiDoc. Semua hak dilindungi.</p>
        </div>
    </footer>

</body>

</html>